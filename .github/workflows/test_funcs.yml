name: test-function-classes

on:
    pull_request:
        branches: [main]

jobs:
    test:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                python-version: ["3.10"]
        steps:
            - uses: actions/checkout@v4

            - uses: ./.github/workflows/conda_cache.yml

            - name: Test function classes
              run: source .github/checks.sh && run_func_checks
